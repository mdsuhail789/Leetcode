class Solution {
public:
    vector<vector<int>> fourSum(vector<int>& nums, int target) {

        int n = nums.size();
        vector<vector<int>>ans;

        // first element 
        for(int n =0; i<n;i++){
            
        }
        if(i>0 && nums[i] ==nums[i-1]);

        // second element 
        for(int j = i+1; i<n; i++){

        }
        for
        
    }
};